#include <iostream>
using namespace std;

int main(){
    int score = 20; // variable declaration (make compiler aware of name and type) and initialization (assign a vlaue). also definition (allocating variable in memory)
    const int uid = 22104039;
    return 0;
}
